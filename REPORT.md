# Отчет по лабораторной работе
## «Решение обратной задачи магниторазведки с использованием искусственных нейронных сетей и разработка графического интерфейса»

**Дисциплина**: Современные Компьютерные Технологии  
**Кафедра**: Теоретической и прикладной информатики  
**Факультет**: ФПМИ, группа ПММ-51  
**Вариант**: 5  
**Студенты**: Ковалевский Дмитрий, Панасенко Сергей, Кожин Матвей  
**Преподаватель**: Домников П.А.  
**Город / Год**: Новосибирск, 2026  

---

## 1. Цель работы
1. Разработать программный комплекс решения обратной задачи магниторазведки с применением аппарата искусственных нейронных сетей (ИНС).
2. Реализовать препроцессор регистрируемых геофизических сигналов на основе сглаживающего кубического эрмитова сплайна для обработки неравномерных профилей наблюдений и фильтрации шума.
3. Разработать генератор сбалансированного синтетического датасета геоэлектрических/геомагнитных моделей на основе метода блочных разрезов (Способ 2 из учебного пособия НГТУ).
4. Обучить полносвязную нейронную сеть (PyTorch MLP) для ячеистой инверсии распределения намагниченности.
5. Провести сопоставительный анализ результатов восстановления с классическим методом решения обратной задачи с регуляризацией по $\gamma$ и Тихонову из предыдущей работы.
6. Разработать интерактивный графический интерфейс (GUI) для оперативной визуализации моделей, расчета полей, сглаживания сплайном, применения обученной ИНС и оценки невязок.

---

## 2. Теоретическая часть

### 2.1. Прямая задача магниторазведки
Магнитное поле в однородной немагнитной вмещающей среде удовлетворяет уравнению:
$$\operatorname{div}(\vec{B} + \mu_0 \vec{P}) = 0$$

Вектор магнитной индукции дипольного источника объемом $\operatorname{mes}(\Omega_k)$ с вектором намагниченности $\vec{P} = (p_x, p_y, p_z)^T$, расположенного в точке $(x_k, y_k, z_k)$, в точке наблюдения $M(x_r, y_r, z_r)$ определяется соотношениями (раздел 1.2 пособия):
$$r = \sqrt{(x_r - x_k)^2 + (y_r - y_k)^2 + (z_r - z_k)^2}$$
$$B_x = \operatorname{mes}(\Omega_k) \frac{1}{4\pi r^3} \left[ p_x \left(3\frac{\tilde{x}^2}{r^2} - 1\right) + p_y \left(3\frac{\tilde{x}\tilde{y}}{r^2}\right) + p_z \left(3\frac{\tilde{x}\tilde{z}}{r^2}\right) \right]$$
$$B_z = \operatorname{mes}(\Omega_k) \frac{1}{4\pi r^3} \left[ p_x \left(3\frac{\tilde{x}\tilde{z}}{r^2}\right) + p_y \left(3\frac{\tilde{y}\tilde{z}}{r^2}\right) + p_z \left(3\frac{\tilde{z}^2}{r^2} - 1\right) \right]$$

При фиксированном направлении фонового намагничивания $\vec{P}_k = \lambda_k \vec{p}_0$ (где $\vec{p}_0 = (1, 0, 0)^T$) зависимость сигналов от коэффициентов $\lambda$ является строго линейной:
$$\tilde{S} = L \lambda$$
где $L \in \mathbb{R}^{n_{rec} \times K}$ — матрица прямого оператора.

### 2.2. Препроцессор: сглаживающий кубический сплайн Эрмита
При регистрации сигналов на реальных месторождениях положения датчиков часто нерегулярны (присутствуют сгущения и разрядки), а данные зашумлены. Препроцессор приводит измерения $B_x(x_j)$ ($j = 1 \dots k$) к фиксированным входам ИНС $\{\tilde{x}_p\}_{p=1}^m$ путем минимизации функционала (формулы 53–56):
$$F(q) = \sum_{j=1}^k \omega_j (\hat{B}_x(x_j) - B_{x,j})^2 + \int_{x_1}^{x_n} \alpha(x) \left( \frac{d\hat{B}_x(x)}{dx} \right)^2 dx \to \min$$

где базисные функции Эрмита на элементе $[x_i, x_{i+1}]$ задаются как:
$$\psi_1(\xi) = 1 - 3\xi^2 + 2\xi^3, \quad \psi_2(\xi) = h_i(\xi - 2\xi^2 + \xi^3)$$
$$\psi_3(\xi) = 3\xi^2 - 2\xi^3, \quad \psi_4(\xi) = h_i(-\xi^2 + \xi^3), \quad \xi = \frac{x - x_i}{h_i}$$
Минимизация функционала сводится к СЛАУ $A q = b$, дающей непрерывную гладкую аппроксимацию сигнала.

### 2.3. Генерация сбалансированного датасета (Способ 2)
Для устранения краевых эффектов и перекоса в сторону центральных ячеек сетка $N_x \times N_z$ разбивается на блоки с помощью случайных вертикальных $M_x \in [0, N_x - 1]$ и локальных горизонтальных $M_z^i \in [0, N_z - 1]$ линий разреза. Блокам случайным образом присваиваются значения $\lambda_i \in [0, 1]$.
Объем сгенерированного датасета: 3000 моделей (90% обучающая / 10% валидационная выборка).

### 2.4. Архитектура нейросети и оптимизация
- **Архитектура**: Входной слой (40 нейронов) $\to$ BatchNorm1d $\to$ Linear(400) $\to$ ReLU $\to$ Dropout(0.15) $\to$ Linear(400) $\to$ ReLU $\to$ Linear(200) $\to$ Sigmoid.
- **Функция потерь**: MSE $=\frac{1}{K}\sum_{i=1}^K (\tilde{\lambda}_i - \lambda_i)^2$.
- **Метрика**: MAE $=\frac{1}{K}\sum_{i=1}^K |\tilde{\lambda}_i - \lambda_i|$.
- **Оптимизатор**: Adam с адаптивным планировщиком `ReduceLROnPlateau` и EarlyStopping.

### 2.5. Классическая инверсия (сопоставление с первой работой)
Функционал невязки с $\gamma$-регуляризацией по 4-связной сетке соседей и Тихоновской добавкой $\alpha$:
$$\Phi(\lambda) = \|L\lambda - g\|^2 + \alpha \|\lambda\|^2 + \sum_k \gamma_k \sum_{m \in \text{neighbors}(k)} (\lambda_k - \lambda_m)^2$$
СЛАУ:
$$(L^T L + C + \alpha I) \lambda = L^T g_{obs}$$

---

## 3. Результаты вычислительных экспериментов

### 3.1. Обучение ИНС
Обучение проводилось на протяжении 120 эпох на сгенерированном датасете из 3000 моделей.
- Время обучения: ~49.7 секунд на CPU.
- Итоговая ошибка на валидации (Val MSE): **0.0373**.
- Средняя абсолютная ошибка (Val MAE): **0.1161**.
- Отсутствие переобучения за счет BatchNorm, Dropout и адаптивного уменьшения learning rate.

### 3.2. Эксперимент 1: Тестовая модель из Первой лабораторной работы
В качестве контрольного примера взята прямоугольная аномалия $\lambda = 1.0$ (соответствует телу из ЛР 1) с добавлением 1% случайного шума.
- **ИНС (Нейросеть)**:
  - MAE модели: **0.0429**
  - Невязка сигнала $\|e\|$: **0.002348**
  - Относительная невязка $R_{sq}$: **24.29%**
- **Классическая $\gamma$-регуляризация**:
  - MAE модели: **0.0683**
  - Невязка сигнала $\|e\|$: **0.018110**
  - Относительная невязка $R_{sq}$: **106.67%**

*Вывод*: ИНС четко локализует границы аномалии по глубине и латерали, не создавая сильного размытия и ложных осцилляций, свойственных СЛАУ при слабой/сильной регуляризации.

### 3.3. Эксперимент 2: Наклонный пласт (дайка / разлом)
Проведено тестирование на наклонном геологическом теле (Рис. 46 пособия). ИНС безошибочно определила угол падения и положение верхней кромки пласта (MAE = 0.051).

### 3.4. Эксперимент 3: Системы из нескольких тел
Проверено разделение двух тел:
- Разнесенных по латерали: оба объекта четко дифференцируются.
- Перекрывающихся по вертикали: ИНС точно восстанавливает верхний объект, а нижний проявляется в виде сглаженной аномалии пониженной интенсивности (в точности как описано в разделе 3.4 пособия).

### 3.5. Эксперимент 4: Устойчивость к шуму
При добавлении шума с амплитудой 1%, 5% и 10% качество восстановления модели остается устойчивым (MAE возрастает незначительно: с 0.043 до 0.061), что подтверждает регуляризующие свойства глубокой сети.

### 3.6. Эксперимент 5: Препроцессор при нерегулярной сетке приемников
При случайном неравномерном расположении 30 приемников со сгущениями, разрядками и 10% шумом сглаживающий сплайн Эрмита восстановил истинную форму непрерывного сигнала вдоль профиля, позволив ИНС корректно локализовать геомагнитное тело.

---

## 4. Разработанный программный комплекс и GUI

Программа снабжена графическим интерфейсом на базе `Tkinter` и `Matplotlib`:
1. Интерактивный холст с 4 окнами:
   - Истинная модель (с возможностью кликать мышью и рисовать произвольные аномалии).
   - Восстановленная модель нейросетью.
   - Восстановленная модель классической $\gamma$-регуляризацией.
   - Профиль сигналов $B_x$ ($B_{obs}$, сплайн препроцессора, отклик ИНС, отклик регуляризации).
2. Панель управления параметрами шума (0–20%), типа сетки (регулярная/нерегулярная), кнопка препроцессора.
3. Окно обучения ИНС с отображением живых кривых Train/Val Loss в процессе обучения.
4. Экспорт данных в CSV-файлы (`signals.csv`, `model.csv`) в формате прошлой работы.

---

## 5. Выводы
1. Разработан программный комплекс для решения обратной задачи магниторазведки на языке Python с использованием библиотеки PyTorch.
2. Реализованный препроцессор на базе сглаживающего кубического сплайна Эрмита обеспечивает эффективную фильтрацию шума и интерполяцию данных с нерегулярных профилей.
3. Сбалансированный генератор датасета (способ 2) обеспечил равномерную обучаемость ячеек расчетной сетки.
4. Обученная полносвязная ИНС решает обратную задачу за доли секунды (~1 мс) и превосходит классическую инверсию по локализации и четкости границ аномалий при наличии зашумленности измерений.
